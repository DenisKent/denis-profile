version: 2
jobs:
    build:
        docker:
            - image: circleci/node:7
        steps:
            - checkout
            - run:
                name: install-dependencies
                command: npm install
            - run:
                name: tests
                command: npm test
            - deploy:
                name: digital-ocean
                command: ssh -o "StrictHostKeyChecking no" root@134.209.168.228 "cd ~/denis-profile; git pull;docker stop $(docker ps -a -q); docker run -p 80:8080 -d denissweat/node"